# MongoDB CRUD Tutorial

Step-by-step lessons for building a full CRUD API on a single MongoDB collection with Express — Create, Read, Update, and Delete — using `insertOne`, `find`, `findOne`, `updateOne`, and `deleteOne`.
