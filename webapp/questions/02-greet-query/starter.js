const express = require('express');
const app = express();

// TODO: add a GET /greet route.
// - If ?name=<value> is provided, respond with `Hello, <value>!`.
// - Otherwise respond with `Hello, stranger!`.

module.exports = app;
